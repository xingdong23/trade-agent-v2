"""Trade Agent 教学示例工程。

建议把这个包理解为“系统总目录”：

- ``apps`` 负责启动与装配；
- ``core`` 提供所有模块共享的运行契约；
- ``agents`` 定义 LangGraph 中的角色边界；
- ``capabilities`` 实现确定性的业务能力；
- ``adapters`` 连接 SQLite、LiteLLM、量化模型等具体技术。
"""

__version__ = "0.1.0"
